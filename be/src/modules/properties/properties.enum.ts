// Define the Property Type enum
export enum PropertyType {
  APARTMENT = 'Apartment',
  VILLA = 'Villa',
  DUPLEX = 'Duplex',
}

// Define the Finishing Status enum
export enum FinishingStatus {
  NOT_FINISHED = 'Not Finished',
  IN_PROGRESS = 'In Progress',
  FINISHED = 'Finished',
}

// Define the Finishing Type enum
export enum FinishingType {
  CORE = 'Core',
  SEMI_FINISHED = 'Semi Finished',
  FULLY_FINISHED = 'Fully Finished',
}
